<div>
  <h1>Genre: {{genre.name}}</h1>

  <div>
    <h4>Books</h4>
    <dl>
      {{#if genre_books}}
        {{#each genre_books}}
        <dt><a href={{url}}>{{title}}</a></dt>
        <dd>{{summary}}</dd>
        {{/each}}
      {{else}}
        This genre has no books.
      {{/if}}
    </dl>
  </div>
</div>