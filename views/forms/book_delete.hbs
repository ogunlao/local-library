<div>
  <h1>{{title}} - {{book.title}}</h1>
  <p>{{book.summary}}</p>

  {{#if book_instances.length}}
  <p><strong>Delete the following  book copies before attempting to delete this book.</strong></p>
  <div>
    <h4>Copies </h4>   
      <dl>
        {{#each book_instances}}
        <dt><a href={{url}}>{{_id}}</a></dt>
        <dd>{{imprint}}</dd>
        <dd>{{status}} - {{due_back_formatted}}</dd>
        <hr>
        {{/each}}
      </dl>
  </div>
  {{else}}
  <p>Do you really want to delete this Book?</p>
  <form action="" method="post">
    <div class="form-group"><input type="hidden" id="bookid" class="form-control" name="bookid" required="true"
        value={{book._id}}></div>
    <button type="submit">Delete</button>
  </form>
  {{/if}}
</div>