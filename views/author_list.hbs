<div>
  <h1>{{title}}</h1>

  <ul>
    {{#if author_list}}
      {{#each author_list}}
      <li><a href={{url}}>{{name}}</a>
        ({{date_of_birth_formatted}} - {{date_of_death_formatted}})
      </li>
      {{/each}}
    {{else}}
    <li>There are no authors.</li>
    {{/if}}

  </ul>
</div>