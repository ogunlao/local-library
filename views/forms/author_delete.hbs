<div>
  <h1>{{title}} - {{author.name}}</h1>
  <p>{{author.lifespan}}</p>

  {{#if author_books.length}}
    <p><strong>Delete the following books before attempting to delete this author.</strong></p>
  <div>
    <h4>Books</h4>
    <dl>
      {{#each author_books}}
      <dt><a href={{url}}>{{title}}</a></dt>
      <dd>{{summary}}</dd>
      {{/each}}
    </dl>
  </div>
  {{else}}
    <p>Do you really want to delete this Author?</p>
    <form action="" method="post">
      <div class="form-group"><input type="hidden" id="authorid" 
        class="form-control" name="authorid" required="true" value={{author._id}}></div>
        <button type="submit">Delete</button>
    </form>
  {{/if}}
</div>