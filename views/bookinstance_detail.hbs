<div>
  <h1>ID: {{bookinstance._id}}</h1>

  <p><strong>Title:</strong><a href={{bookinstance.book.url}}>{{bookinstance.book.title}}</a></p>
  <p><strong>Imprint:</strong> {{bookinstance.imprint}}</p>

  <p><strong>Status:</strong>
    {{#if (eqw bookinstance.status 'Available')}}
    <span class="has-text-success">{{ bookinstance.status}}</span>
    {{else}}
    {{#if (eqw bookinstance.status 'Maintenance')}}
    <span class="has-text-warning">{{bookinstance.status}}</span>
    {{else}}
    <span class="has-text-danger">{{bookinstance.status}}</span>
    {{/if}}
    {{/if}}
    
    {{#if (eqw bookinstance.status 'Available')}}
    <p><strong>Due back:</strong> {{bookinstance.due_back_formatted}} </p>
    {{/if}}
  </p>

  <hr>
  <p><a href="{{bookinstance.url}}/delete">Delete this copy</a></p>
  <p><a href="{{bookinstance.url}}/update">Update this copy</a></p>

</div>